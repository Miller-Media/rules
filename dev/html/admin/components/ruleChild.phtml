<ips:template parameters="$rule" />

<div class='ipsMessage ipsMessage_warning rulesMessage'>
	This rule will be evaluated only if the conditions of it's 
	{{if $rule}}<a href='{url="app=rules&module=rules&controller=rules&do=form&id={$rule->parent()->id}&tab=conditions"}'>parent rule</a> 
	{{else}}parent rule
	{{endif}}
	are also met.
</div>